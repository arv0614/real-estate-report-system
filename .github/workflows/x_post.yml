# X Auto Post — DISABLED (2026-05-09)
#
# Xへの自動投稿は停止しました。
# 元のスケジュール (月・水・金 JST 09:00) と post ステップは下記にコメントアウトのみ残してあります。
# 復活させたい場合は本ファイル全体を git history から復元してください。

name: X Auto Post (Disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    name: Disabled (no-op)
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Notice
        run: |
          echo "::notice::X Auto Post is disabled. See .github/workflows/x_post.yml for history."

# ─── 旧設定（無効化済み） ─────────────────────────────────────────────────────
# on:
#   # 月・水・金 JST 09:00 (UTC 00:00 = 1=Mon, 3=Wed, 5=Fri)
#   schedule:
#     - cron: "0 0 * * 1,3,5"
#   workflow_dispatch:
#
# jobs:
#   post:
#     name: Post to X
#     runs-on: ubuntu-latest
#     timeout-minutes: 5
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "npm"
#       - name: Install dependencies
#         run: npm ci
#       - name: Post tweet
#         env:
#           X_API_KEY: ${{ secrets.X_API_KEY }}
#           X_API_SECRET: ${{ secrets.X_API_SECRET }}
#           X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
#           X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
#         run: node scripts/post_to_x.js
